# Core dependencies
pydantic>=2.0
pyyaml>=6.0
typer>=0.12
rich>=13.0

# Data loading
datasets>=2.19
huggingface-hub>=0.23

# Deduplication
datasketch>=1.6
xxhash>=3.0

# Text chunking
langchain-text-splitters>=0.2
tiktoken>=0.7

# Embeddings & ANN
sentence-transformers>=3.0
faiss-cpu>=1.8

# Clustering & dataframes
hdbscan>=0.8
polars>=0.20
pyarrow>=16.0

# Checkpointing
duckdb>=0.10

# Quality filtering
sympy>=1.12
json-repair>=0.25

# HTTP / async
httpx>=0.27
tenacity>=8.3

# Export
orjson>=3.10

# ── System tools (installed via conda, not pip) ──────────────────────────────
# nginx — load balancer for multi-node vLLM (vllm/slurm_serve_multi.sh)
#   conda install -c conda-forge nginx
#   (setup_env.sh Phase 2 does this automatically)

# ── Optional extras ──────────────────────────────────────────────────────────
# GPU inference (CUDA — laptop dev):
#   pip install torch>=2.3 vllm>=0.5
#
# Fast GPU clustering (requires CUDA/ROCm + Triton):
#   pip install flash-kmeans
#
# GPU inference (ROCm — cluster):
#   torch installed by setup_env.sh Phase 3 (ROCm wheel, not pip default)
#   vllm runs from Singularity SIF — see vllm/build_sif.sh, not pip install
#
# Code sandbox (production):
#   pip install e2b-code-interpreter>=0.0.8
#
# Translation (v2):
#   pip install deepl>=1.18 google-cloud-translate>=3.15

# ── Dev / test ────────────────────────────────────────────────────────────────
# pytest>=8.0
# pytest-asyncio>=0.23
# numpy>=1.26
# ruff>=0.4
# mypy>=1.10
