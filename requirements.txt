# Core dependencies
pydantic>=2.0
pyyaml>=6.0
typer>=0.12
rich>=13.0

# Data loading
datasets>=2.19
huggingface-hub>=0.23

# Deduplication
datasketch>=1.6
xxhash>=3.0

# Text chunking
langchain-text-splitters>=0.2
tiktoken>=0.7

# Embeddings & ANN
sentence-transformers>=3.0
faiss-cpu>=1.8

# Clustering & dataframes
hdbscan>=0.8
polars>=0.20
pyarrow>=16.0

# Checkpointing
duckdb>=0.10

# Quality filtering
sympy>=1.12
# Required by sympy.parsing.latex.parse_latex (the math verifier). Without it
# every LaTeX answer is treated as "uncertain" and the math filter no-ops.
# sympy 1.13/1.14 require the 4.11 line specifically.
antlr4-python3-runtime>=4.11,<4.12
json-repair>=0.25

# LLM API clients — Stage 3 annotator, Stage 5 openai_api mode, LLM judge
openai>=1.40

# HTTP / async
httpx>=0.27
tenacity>=8.3

# Export
orjson>=3.10

# ── System tools (installed separately, not via pip or the pipeline overlay) ─
# nginx — load balancer for vllm/slurm_serve_array.sh (task 0 runs it on the host).
# Needs its own conda prefix on scratch so it's accessible outside Singularity:
#   conda create -p /scratch/project_462000963/users/aralikatte/sft-nginx \
#       -c conda-forge nginx -y

# ── Optional extras ──────────────────────────────────────────────────────────
# GPU inference (CUDA — laptop dev):
#   pip install torch>=2.3 vllm>=0.5
#
# Fast GPU clustering (requires CUDA/ROCm + Triton):
#   pip install flash-kmeans
#
# GPU inference (ROCm — cluster):
#   torch installed by setup_env.sh Phase 3 (ROCm wheel, not pip default)
#   vllm runs from Singularity SIF — see vllm/build_sif.sh, not pip install
#
# Code sandbox (production):
#   pip install e2b-code-interpreter>=0.0.8
#
# LLM judge with Anthropic models (scripts/llm_judge_eval.py --provider anthropic):
#   pip install anthropic>=0.34
#
# Verifier sanity-check harness + comparison viz (sanity-checks/):
#   pip install -r sanity-checks/requirements.txt
#
# Translation (v2):
#   pip install deepl>=1.18 google-cloud-translate>=3.15

# ── Dev / test ────────────────────────────────────────────────────────────────
# pytest>=8.0
# pytest-asyncio>=0.23
# numpy>=1.26
# ruff>=0.4
# mypy>=1.10
